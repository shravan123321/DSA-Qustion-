//define a circular linked list to implements cll data structure with member variable last pointer of type node
#include<iostream>
using namespace std;

struct node{
    int item;
    node* next;
};

class circularlinkedlist{
    private:
      node* last;
    public:
      
};